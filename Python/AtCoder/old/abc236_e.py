# 貰うDP
# 平均値は、
# 平均値は、平均値を持っておけばよい
# 中央値は、中央値より大きい値は常に採用していることから
# 中央値だけから、次の中央値を復元できる
def main():
    n = int(input())
    A = list(map(int, input().split()))

    # 平均値
    # Aのうち、大きいものから潰していく？


main()
